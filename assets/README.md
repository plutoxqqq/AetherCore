# Assets

Binary assets are intentionally not committed to this repository so pull requests stay lightweight and easy to create.

The `new`, `old`, `rise`, and `wurst` folders are kept for VapeV4-style path compatibility. Runtime code may fetch remote assets when an executor supports file caching, but source changes should avoid adding `.png`, `.ttf`, `.otf`, or other binary files directly to Git.
